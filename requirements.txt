# pyproject.toml is the single source of truth for dependencies.
# This file exists only so that `pip install -r requirements.txt` does the right
# thing: it installs this package with the [full] extra, exactly like the
# installers do. Run it from the repository root: pip resolves the "." below
# against the current directory, not against this file.
#
# Recommended install:
#   macOS / Linux   ./install.sh --all
#   Windows         powershell -ExecutionPolicy Bypass -File .\install.ps1 --all
#
# NVIDIA on Windows: install CUDA torch FIRST, because PyPI's Windows torch
# wheel is CPU-only:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128

-e .[full]
